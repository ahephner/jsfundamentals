//this method .join(); returns the value of the array as a string
var myArray=[23,"AJ", "Jackie"];

console.log(myArray.join());

//using the ("+"); adds strings together
console.log(myArray.join("+"));

// this (""); this combines elements into one string

console.log(myArray.join(""));